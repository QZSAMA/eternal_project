# 程序员求婚 Galgame 网页游戏 — 实现计划

## 一、概要

用一个自包含、离线运行的网页 Galgame（视觉小说），把“在守望先锋双排中相识 → 游戏走到现实 → 求婚”的故事讲给她看。剧情推进到“组排打游戏”段落时，嵌入一个可玩的守望先锋主题 Mini 小游戏（本地 Three.js 主渲染，Canvas 2D/skip 降级）。最终场景自动播放求婚动画，她亲手点下“我愿意”。

**核心约束**：
- 离线优先、零运行时网络依赖；第三方库必须本地 vendoring，并保留不依赖 WebGL 的降级链路
- 投屏友好：1920×1080 基准，等比缩放适配电视/投影
- 程序员可配置：所有文案、照片、BGM 集中在 `storyData.js`，占位符用 `{{...}}`
- 用户稍后提供真实照片；先用 Seedream 生成统一画风的二次元占位素材
- 不使用暴雪官方版权素材，守望风格仅做视觉语言提炼

---

## 二、当前状态分析

工作目录为空，从零开始搭建。用户提供的关键信息：
- 程序员，工作时间长（日均15小时），需要筹备量小、可利用代码技能完成的方案
- 无车、出租屋不浪漫，排除了家中/户外/旅行寻宝方案
- 两人通过守望先锋认识，喜欢二次元
- 希望带上电脑投屏到酒店房间即可求婚
- 选择了"思路C：视觉小说/Galgame 风"
- 要求剧情中有守望先锋 Mini 小游戏环节

---

## 三、技术选型

| 层面 | 选择 | 理由 |
|---|---|---|
| 引擎 | 原生 JS 声明式剧情解释器（借鉴 Ren'Py 语义） | 轻量、离线、无构建，约400-600行覆盖需求 |
| 小游戏 | 本地 Three.js `r160` + Canvas 2D + skip | 保留 3D 表现；断网、无 WebGL 或无 2D context 时仍能继续主线 |
| 音频 | Web Audio API + `<audio>` 元素 | 支持合成音效与外部MP3，开始按钮解锁自动播放 |
| 布局 | 固定1920×1080舞台 + `transform: scale()` 等比缩放 | 投屏不变形，字号有保障 |
| 素材 | Seedream 生成二次元占位 + 用户真实照片回退 | 先可演示，后替换 |

**不使用**：Monogatari/Pixi/Phaser 等额外框架；CDN、远程字体和在线 API；暴雪官方素材（版权）。Three.js 是唯一第三方运行时，锁定版本并放在 `vendor/`。

---

## 四、文件结构

```
proposal-galgame/
├── index.html                  # 入口，DOM结构与引擎挂载点
├── css/
│   └── style.css               # 全部样式：对话框、立绘、转场、OW主题、缩放
├── js/
│   ├── engine.js               # 剧情解释器、场景/立绘/对话框系统、输入控制
│   ├── configValidation.js     # CONFIG/剧情引用启动校验
│   ├── minigameMode.js         # Three.js/2D/skip 模式选择（纯函数）
│   ├── minigame.js             # 守望主题 Three.js + Canvas 2D 小游戏
│   ├── audio.js                # BGM/SFX 管理（Web Audio，自动播放解锁）
│   └── storyData.js            # ★ 用户主要配置文件：剧情、选项、素材、文案
├── vendor/
│   └── three-r160.min.js       # 本地锁定 Three.js
├── tests/                      # 配置契约与降级模式回归测试
├── assets/
│   ├── images/
│   │   ├── backgrounds/        # 场景背景（二次元占位）
│   │   ├── characters/         # 人物立绘（透明PNG，多表情）
│   │   ├── ui/                 # UI装饰元素
│   │   ├── photos/             # ★ 用户真实照片（替换占位）
│   │   └── minigame/           # 小游戏贴图
│   └── audio/
│       ├── bgm/                # 背景音乐
│       └── sfx/                # 音效
└── README.md                   # 填写指南：占位符清单、照片尺寸、使用说明
```

---

## 五、剧情数据结构

### 5.1 顶层 CONFIG

`storyData.js` 导出 `CONFIG` 对象，包含：
- `meta`：标题、两人名字、求婚日期
- `audio`：BGM/SFX 文件路径
- `images`：背景、立绘、照片、UI、小游戏贴图路径
- `story`：剧情脚本（label → 指令数组）

### 5.2 指令集

| 指令 | 必填字段 | 说明 |
|---|---|---|
| `scene` | `bg`, `effect`(fade/zoomin/slide) | 切换背景 |
| `show` | `char`, `pos`(left/center/right) | 立绘入场 |
| `hide` | `char`（`*`=全部） | 立绘退场 |
| `say` | `text`, `who`(角色id/narration) | 台词，打字机效果 |
| `menu` | `prompt`, `options:[{text,next}]` | 分支选择 |
| `jump` | `label` | 跳转label |
| `call` | `minigame` | 调用小游戏，结束后继续 |
| `wait` | `ms` | 停顿 |
| `bgm` | `track`, `fade`? | 背景音乐切换 |
| `sfx` | `name` | 音效 |
| `effect` | `type`(flash/shake/spotlight) | 屏幕特效 |
| `montage` | `slides:[{img,caption}]`, `bgm`? | 照片轮播 |
| `proposal` | — | 终局求婚交互 |

### 5.3 剧情章节（label）

1. **start**：开篇，守望主菜单风格背景，旁白介绍相识
2. **duo_queue**：双排日常，语音聊天，感情升温
3. **分支选项**："一起打排位冲分" → gaming / "出来见个面" → first_date
4. **gaming**：调用 Mini 游戏（守点60秒），胜利后女主夸奖
5. **first_date**：第一次见面的场景与对话
6. **from_game_to_real**（分支汇合）：从游戏走到现实的告白
7. **montage**：真实照片蒙太奇 + 短句旁白
8. **proposal**：自动播放求婚时间线，戒指出现，"我愿意/让我想想"按钮

---

## 六、核心系统设计

### 6.1 场景/背景系统
- 双层 `<img>` 交叉淡入淡出
- 支持 `zoomin`（Ken Burns 缓慢推进）和 `fade` 转场
- 真实照片探测：启动时用 `Image.onerror` 检查照片是否存在，有则替换二次元占位

### 6.2 立绘系统
- 三层定位槽（left/center/right），透明PNG半身像
- 入场：下方40px上移 + 淡入（300ms）
- 呼吸动画：`scale(1)↔scale(1.015)`，周期3.5s，错峰
- 说话高亮：当前角色 brightness 提升 + 橙色光晕，其他角色压暗
- 情绪切换：同位置短淡出+淡入

### 6.3 对话框系统
- 底部28%高度，半透明深色底 + `backdrop-filter: blur`
- 橙色边框 + 四角菱形装饰
- 名牌：斜切色块，说话角色主题色
- 打字机效果：逐字显示，点击补全，句末闪烁"▼"
- 推进输入：点击/空格/回车/方向键/触屏

### 6.4 选项系统
- 半透明遮罩 + 居中面板，按钮逐条延迟淡入
- 斜切角按钮，hover橙色发光
- 键盘可操作（上下选、回车确认）
- 所有分支汇合到同一label

### 6.5 转场与特效
- fade（黑幕过渡）、flash（白闪）、shake（屏幕抖动）、spotlight（暗角聚焦）
- 轻量Canvas粒子层：樱花/光斑/金色光点，按章节切换，≤150颗粒子

### 6.6 音频系统
- 开场"点击开始"按钮解锁 AudioContext（绕过浏览器自动播放限制）
- BGM淡入淡出切换（1.5s）
- SFX：点击、选项、枪火、命中、占点、心跳、钟声
- 右上角静音按钮
- 未提供音频时用Web Audio合成简单音效

### 6.7 响应式与投屏
```js
function fit(){
  const s = Math.min(innerWidth/1920, innerHeight/1080);
  stage.style.transform = `translate(-50%,-50%) scale(${s})`;
}
```
- 所有UI尺寸基于1920画布用px，不混用vw
- 电视安全区：关键内容距边缘≥80px
- 支持鼠标/键盘/触屏/翻页笔

---

## 七、守望先锋 Mini 游戏设计

### 7.1 游戏形态
- **名称**："守护目标点60秒"
- **类型**：俯视角简化射击 + 占点进度
- **时长**：45-60秒（可配置）
- **操作**：WASD移动，鼠标瞄准+左键射击；触屏有虚拟摇杆
- **失败不卡关**：HP归零触发"她复活了你"继续；超时未占满她说"和你一起玩就很开心"继续

### 7.2 核心机制
- **玩家**：几何体+男主头像图标，HP100，射速6发/秒
- **控制点**：屏幕中央圆环，玩家在内占点进度每秒+4-6%，达100%胜利
- **敌人Bot**：边缘生成，向玩家/控制点移动，碰撞造成伤害
- **"她的治疗"**：右侧女主头像每8秒释放治疗光束，回复HP，飘字"+♥ 她在奶你"
- **HUD**：顶部占点进度条（守望分段刻度）、左下血条、右下倒计时、右上小地图

### 7.3 视觉表现
- 暗色科技网格地面（Canvas绘制透视网格）
- OW配色：亮橙`#F99E2A`、深蓝灰`#1B2838`、青蓝`#4FC3F7`、警示红`#E44040`
- 粒子：命中火花、占点光环扩散、击杀爆炸
- 受伤屏幕震动，胜利闪白+"VICTORY"扫光
- 右上角"跳过"按钮，防止现场无键鼠

---

## 八、求婚终局设计

### 8.1 自动播放时间线
1. 背景切求婚现场，Ken Burns推进，BGM淡入
2. 立绘退场，画面让给真实场景
3. 告白旁白4-6句，逐字打出，心跳音效渐强
4. spotlight聚焦，可选播放预录告白视频
5. CSS/SVG戒指升起+旋转+金色光晕，"嫁给我，好吗？"
6. 两个按钮浮入："我愿意 ♥"（大、橙色、脉冲、默认焦点）/ "让我想想…"（小、灰）

### 8.2 "拒绝"按钮彩蛋
- hover/点击时按钮随机跑开，文字变"再想想？""不许选这个哦"等
- 或弹出"（系统提示：该选项在当前剧情线不可用，因为他真的很爱你）"
- 键盘方向键尝试移过去也触发逃跑
- 确保现场零尴尬

### 8.3 "我愿意"之后
- 闪白 → 金色粒子爆发 → 屏幕震动 → 温馨音效
- 大字"她说好！" / "THE ANSWER IS YES"
- 结尾画面：合照 + "Chapter 1 完 / 我们的故事，才刚刚开始" + 日期
- 樱花飘落，"按任意键重新开始"

---

## 九、视觉风格

### 9.1 配色
- 守望主色：亮橙 `#F99E2A` / `#FFB13B`
- 深色背景：`#1B2838` / `#2A475E`
- 强调青蓝：`#4FC3F7`
- 求婚暖金：`#FFD700`
- Galgame对话框：`rgba(10,15,25,.82)`

### 9.2 UI元素
- 斜切角面板（`clip-path: polygon(...)`）
- 六边形/菱形装饰
- 扫描线纹理、科技网格底纹
- 橙色发光（text-shadow/box-shadow）
- 对话框四角菱形装饰

### 9.3 字体
- 标题：粗黑无衬线 + 字间距拉开 + 橙色描边
- 正文：圆润sans-serif，`clamp(20px, 2.2vw, 32px)`（基于1920画布实际用px）
- 最多两种字体

---

## 十、素材清单

### 10.1 Seedream 生成（二次元占位）

**场景背景**（1920×1080，二次元插画，无文字）：
1. `ow_menu.jpg`：守望主菜单风格，深蓝夜空+橙色光效+科技城市剪影
2. `ow_route66.jpg`：66号公路风格荒漠日落，二次元化
3. `chat_night.jpg`：深夜卧室/电脑桌，屏幕光+月光，温暖氛围
4. `first_date.jpg`：咖啡店/商业街黄昏，二次元城市街景
5. `park.jpg`：春天公园樱花道
6. `proposal.jpg`：夜景天台/酒店落地窗，暖色灯光，浪漫氛围

**人物立绘**（透明背景PNG，半身，1200×1600，统一赛璐璐画风）：
- 男主3张：`hero_neutral.png`（日常连帽衫）、`hero_smile.png`、`hero_serious.png`（游戏中/告白）
- 女主3张：`heroine_normal.png`、`heroine_laugh.png`、`heroine_shy.png`（被求婚时）
- 风格：日系二次元、赛璐璐、干净线稿、柔和上色、视觉小说立绘

**Mini游戏素材**（256×256图标）：
- 玩家英雄图标（男主icon版）
- 治疗头像（女主icon版）
- 敌人Bot图标（简洁机械单位）

**UI/粒子**：
- 樱花花瓣贴图
- 金色光点贴图
- 戒指SVG（CSS绘制为主）

### 10.2 用户提供（真实素材）
- 照片6-10张：相识纪念、约会/旅行合照、结尾合照（放入`photos/`）
- 戒指照片（可选）
- BGM 4段：开场/游戏/约会/求婚（可选，不提供则合成）
- 告白视频（可选）
- 所有文案（在`storyData.js`的`{{...}}`占位符中填写）

---

## 十一、实现步骤

1. **搭建舞台骨架**：`index.html` + `style.css` + `fit()` 缩放，1920×1080舞台居中
2. **引擎核心**：`engine.js` 指令解释器，支持 `scene/show/hide/say/jump`，假数据跑通
3. **对话框与打字机**：打字机效果、点击补全、句末提示、名牌、旁白模式
4. **选项与分支**：`menu` UI、键盘操作、分支跳转与汇合
5. **转场特效与粒子**：fade/flash/shake/spotlight、Canvas粒子层、立绘呼吸与高亮
6. **音频与开始界面**：开始按钮解锁音频、BGM淡入淡出、SFX、静音按钮
7. **Seedream素材生成与接入**：生成背景/立绘/图标，放入路径，验证视觉
8. **Mini游戏**：`minigame.js`，玩家移动+射击+占点+Bot+HUD+粒子+音效，接入`call`指令，跳过/失败继续
9. **蒙太奇指令**：照片轮播+Ken Burns+字幕，真实照片探测与回退
10. **求婚终局**：auto时间线、戒指动画、接受/拒绝交互（逃跑彩蛋）、"我愿意"爆发、结尾画面
11. **配置化与文档**：文案/路径抽到`storyData.js`，打`{{...}}`占位符，写`README.md`填写指南
12. **投屏实测优化**：1920×1080/4K测试字号、输入方式、性能、离线运行；调整节奏
13. **打包交付**：文件夹形态（U盘可携），可选单HTML内联备份

---

## 十二、假设与决策

- **假设**：用户使用Chrome/Edge浏览器在笔记本上打开，通过HDMI投屏到酒店电视
- **假设**：用户能提供至少6-10张照片和基本文案；Seedream占位素材保证无照片时也能完整演示
- **决策**：采用轻量项目结构而非单HTML文件，便于用户替换照片和文案；最终可选打包单文件备份
- **决策**：Mini游戏失败不卡关，因为求婚现场不能让游戏技巧成为障碍
- **决策**："拒绝"按钮设计为逃跑彩蛋而非真实选项，确保零尴尬
- **决策**：不使用任何暴雪官方素材/音乐/角色名，仅提炼"橙色+斜切角+科技感+占点"等通用视觉语言
- **决策**：剧情文案用`{{中文描述}}`占位符，用户自行填写真实台词（真实最打动人）

---

## 十三、验证步骤

1. 双击 `index.html`，断网状态下完整运行从开始到"我愿意"结尾
2. 剧情数据全部在 `storyData.js`，不改引擎代码即可替换全部文案、照片、BGM
3. 未提供真实素材时，二次元占位素材完整呈现，无破图/空白
4. Mini游戏可玩、可跳过、不卡关，结束后正确回到剧情
5. 求婚场景自动播放，"我愿意"有高潮反馈，"拒绝"是温柔彩蛋
6. 1920×1080电视投屏下字号清晰、UI不贴边、动画流畅、无滚动条
7. 文件夹整体拷贝到U盘，在任意Windows/Mac + Chrome/Edge即开即用
8. 支持鼠标/键盘/触屏三种输入方式，回车键可一路推进到"我愿意"
